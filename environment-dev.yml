name: sparrow-rockfinch
channels:
  - conda-forge
dependencies:
  # Build
  - cmake
  - ninja
  # Dependencies
  - sparrow-devel
  - python
  - nanobind
  - nanobind-abi
  # Tests
  - doctest
  - polars
  - pyarrow
  - pytest
  # Documentation
  - doxygen
  - graphviz
